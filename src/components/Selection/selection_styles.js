import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  selectCard: {
    paddingTop: '18px',
  },
  selectors: {
    textAlign: 'center',
    display: 'flex',
    paddingTop: '4px',
    paddingBottom: '8px',
  },
  select: {
    fontSize: '.875em',
    marginRight: '5px',
    marginTop: '0px',
    marginBottom: '0px',

  },
  selectionPrompt: {
    paddingTop: '24px'
  }
});

export default useStyles;